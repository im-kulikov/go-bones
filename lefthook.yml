# lefthook.yml

pre-commit:
  parallel: true
  jobs:
    - name: deps & lint & test
      glob: "*.go"
      group:
        jobs:
          - run: go mod tidy

          - run: make lint
            stage_fixed: true

          - run: make test